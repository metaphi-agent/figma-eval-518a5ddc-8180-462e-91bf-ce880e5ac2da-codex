# Registration UI Templates (React)

Production-grade React + Vite + Tailwind v4 implementation of the Figma file:
“Registration UI templates (Sign up/Log in) (Community)”.

## Dev

```bash
npm install
npm run dev
```

Open `#/screens` to jump between all screens.

## Build

```bash
npm run build
```

